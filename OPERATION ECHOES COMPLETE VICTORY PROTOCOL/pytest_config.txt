[pytest]
# Pytest configuration for Echoes project

# Test discovery patterns
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

# Minimum Python version
minversion = 7.0

# Add current directory to PYTHONPATH
pythonpath = .

# Test paths
testpaths = tests

# Markers for selective test execution
markers =
    application: Core application functionality tests (critical path)
    core: Statistical framework tests (non-critical during development)
    integration: Integration tests requiring external services
    slow: Tests that take >5 seconds
    gpu: Tests requiring GPU acceleration
    network: Tests requiring network access

# Output configuration
addopts =
    -ra
    --strict-markers
    --strict-config
    --showlocals
    --tb=short
    --maxfail=10
    
# Timeout for hanging tests (requires pytest-timeout)
timeout = 300
timeout_method = thread

# Coverage configuration (if pytest-cov installed)
# Uncomment when ready for coverage tracking
# --cov=echoes
# --cov=api
# --cov-report=html
# --cov-report=term-missing

# Ignore patterns
norecursedirs = 
    .git
    .tox
    dist
    build
    *.egg
    __pycache__
    .venv
    venv

# Filter warnings
filterwarnings =
    error
    ignore::UserWarning
    ignore::DeprecationWarning
    ignore:.*DEPRECATION.*:
    
# Log configuration
log_cli = false
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# Asyncio configuration
asyncio_mode = auto
