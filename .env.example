# Copy this file to .env and adjust values as needed.
# Network egress policy (default: enforce deny-all, allow only 'openai')
EGRESS_ENFORCE=1
EGRESS_ALLOWLIST=openai
EGRESS_LOG=1
EGRESS_AUTOPATCH=1

# OpenAI configuration (only used when explicitly allowed by policy)
OPENAI_API_KEY=
# Optional override of API base URL (host will still be checked against allowlist)
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_BASE=https://api.openai.com/v1

# Application settings (examples)
APP_ENV=development
PYTHONHASHSEED=0
