# ========================
# AI Advisor API Configuration
# ========================
# Copy this file to .env and update values as needed
# DO NOT commit sensitive data to version control

# Server Configuration
# -------------------
# Host to bind to (use 127.0.0.1 for development, 0.0.0.0 for production with proper firewall rules)
HOST=127.0.0.1

# Port to run the server on
PORT=8000

# Enable auto-reload (set to false in production)
RELOAD=true

# Logging level (debug, info, warning, error, critical)
LOG_LEVEL=info

# Security
# --------
# Secret key for session encryption (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-here

# CORS (comma-separated list of allowed origins, or * for development only)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Database
# --------
# Database connection string (SQLAlchemy format)
DATABASE_URL=sqlite:///./ai_advisor.db

# API Keys (if any)
# OPENAI_API_KEY=your-openai-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key

# Feature Flags
# ------------
# Enable/disable specific features
FEATURE_ANALYTICS=false
FEATURE_DEBUG_TOOLS=false
