# Core Dependencies
# Policy: No telemetry/analytics SDKs are included by default (aligns with "knock first / be transparent").
# Versions are minimally pinned with lower bounds to avoid breaking changes while enabling security patches.
openai>=1.0.0
python-dotenv>=0.19.0
requests>=2.26.0

# Web Framework (if using FastAPI/Flask)
fastapi>=0.68.0
uvicorn>=0.15.0

# Data Processing
pandas>=1.3.0
numpy>=1.21.0

# Utils
python-dateutil>=2.8.2
pydantic>=1.8.0

# Development
black>=21.7b0
isort>=5.9.3
mypy>=0.910
ruff>=0.1.6
pre-commit>=3.7.0
pytest>=7.0.0
pytest-cov>=2.12.1
pytest-socket>=0.6.0

# Optional Dependencies (uncomment if needed)
# duckduckgo-search>=2.9.0
# psutil>=5.9.0
# watchdog>=2.1.6
# matplotlib>=3.4.3
# jupyter>=1.0.0
