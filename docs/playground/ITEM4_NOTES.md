# Item 4 — reserved for your fourth dimension

The original playbook listed a fourth bullet that was incomplete (`[TAB]`).

When you define it (e.g. another evaluation axis, integration gate, or migration checkpoint), add it here or extend [`CAPABILITY_MATRIX.md`](CAPABILITY_MATRIX.md) with new columns or a sibling matrix file under `docs/playground/`.
