Fix the current pull request branch inside the Echoes repository.

Constraints:

- Work only on this PR branch.
- Do not merge or rebase.
- Use `uv`, not raw `pip`.
- Avoid widening scope into unrelated research or archival scripts.
- Run only the trusted validation command after making changes.

Restore green checks with the smallest safe change set.
